stages:
  - zipContainerInfo

zipContainerInfo:
  stage: zipContainerInfo
  tags:
    - S4-B
  script: 
    - name="shenhangke"
    - cd ${SHARED_DIR}/Builds/${name}/DataReposity
    - tar -zcvf ItemContainerInfo.tar.gz ItemContainerInfo
    - split -b 400m -d ItemContainerInfo.tar.gz ItemContainerInfo.tar.gz_
  when: always